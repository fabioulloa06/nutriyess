{
  "builds": [
    {
      "src": "main.py",
      "use": "@railway/python"
    }
  ],
  "deploy": {
    "startCommand": "uvicorn main:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300
  }
}
