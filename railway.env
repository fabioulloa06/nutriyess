# Railway Environment Configuration
# Variables de entorno para Railway

# Database (Railway proporciona automáticamente DATABASE_URL cuando agregas PostgreSQL)
# Si no hay DATABASE_URL, usará SQLite local

# Environment
ENVIRONMENT=production
DEBUG=False

# Security (cambia estos valores en producción)
SECRET_KEY=nutriyess-secret-key-change-in-production-2024
JWT_SECRET_KEY=nutriyess-jwt-secret-key-change-in-production-2024

# CORS Configuration
ALLOWED_ORIGINS=https://nutriyess.vercel.app,https://nutriyess.netlify.app,http://localhost:3000

# Port (Railway asigna automáticamente)
PORT=$PORT

# Logging
LOG_LEVEL=info